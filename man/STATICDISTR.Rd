% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/staticdistr.R
\name{STATICDISTR}
\alias{STATICDISTR}
\title{Static Count Distribution Model}
\usage{
STATICDISTR(
  formula,
  distr = c("auto", "pois", "hsp", "nbinom", "hsnb", "mixture"),
  hot_start = FALSE,
  criterion = c("aic", "bic"),
  ...
)
}
\arguments{
\item{formula}{Model specification.}

\item{distr}{Distribution choice: one of \code{"auto"}, \code{"pois"}, \code{"hsp"},
\code{"nbinom"}, \code{"hsnb"}, or \code{"mixture"}.}

\item{hot_start}{Logical. If \code{TRUE}, leading zeros are removed from the
time series before fitting.}

\item{criterion}{Information criterion to use for model selection when \code{distr = "auto"}. One of \code{"aic"} or \code{"bic"}.}

\item{...}{Not used.}
}
\value{
A model specification.
}
\description{
Static (IID) count distribution model for intermittent demand, following
Kolassa (2016). The method fits several candidate distributions --- Poisson,
hurdle-shifted Poisson, negative binomial, and hurdle-shifted negative
binomial --- to the observed series and selects the best by AIC. A mixture
option that blends all four predictive distributions is also available.
}
\examples{
ts <- tsibble::tsibble(
  time = as.Date("2026-01-01") + seq_len(40),
  value = rnbinom(40, size = 1, prob = 0.3),
  index = time
)

fc_ts <- ts |>
  model(STATICDISTR(value)) |>
  forecast(h = "7 days")

fc_ts |> print()


if (requireNamespace("ggtime", quietly = TRUE)) {
  library(ggtime)
  fc_ts |> autoplot(ts)
}
}
\references{
Kolassa, S. (2016). Evaluating predictive count data distributions in retail
sales forecasting. \emph{International Journal of Forecasting}, 32(3), 788--803.
\doi{10.1016/j.ijforecast.2015.12.004}.
}
